A simple OpenFlow proxy. 
To use, run the example mininet topology (sudo python startTopo.py), start the example controller (ryu-manager exampleController.py), then run the proxy (sudo python ofProxy.py)

python package requirements:
ryu
twink